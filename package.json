{
  "name": "inex-ge-userscript",
  "version": "0.4.15",
  "private": true,
  "description": "Violentmonkey userscript for inex.ge parcels page.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "node --test scripts/*.test.mjs",
    "stylelint": "stylelint \"src/**/*.css\"",
    "stylelint:fix": "stylelint \"src/**/*.css\" --fix",
    "smoke:build": "node scripts/check-build.mjs",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "prettier": "^3.8.3",
    "stylelint": "^17.11.0",
    "stylelint-config-standard": "^40.0.0",
    "vite": "^8.0.11",
    "vite-plugin-monkey": "^8.0.0"
  }
}
